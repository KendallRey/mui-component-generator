{{ #directive }}
{{{ directive }}}
{{ /directive }}
import React, { memo } from 'react';
import { {{name}}, {{name}}Props } from '@mui/material';

type {{prefix}}{{name}}Props = {{name}}Props;

export const {{prefix}}{{name}}: React.FC<{{prefix}}{{name}}Props> = memo((props) => <{{name}} {...props} />);
