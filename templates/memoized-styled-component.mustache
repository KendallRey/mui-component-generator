{{ #directive }}
{{{ directive }}}
{{ /directive }}
import React, { memo } from 'react';
import { styled, {{name}}, {{name}}Props } from '@mui/material';

type {{customName}}Props = {{name}}Props;

export const {{customName}} = memo(
  styled(({ ...props }: {{customName}}Props) => (<{{name}} {...props} />))(({ theme }) => ({}))
);

type {{prefix}}{{name}}Props = {{customName}}Props;

export const {{prefix}}{{name}}: React.FC<{{prefix}}{{name}}Props> = memo(
  (props) => <{{customName}} {...props} />
);

